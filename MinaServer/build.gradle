
allprojects {
  apply plugin: "java"

  version = 1.0

  repositories {
      mavenCentral()
  }
}

subprojects {

}

dependencies {
  compile project(':MinaAuth')
  compile project(':MinaGame')
}
